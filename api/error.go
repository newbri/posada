package api

const (
	ErrBindToJSON       = "type cannot be bind to json"
	ErrUniqueViolation  = "identifier must be unique"
	ErrInternalServer   = "an error occurs"
	ErrNoRow            = "no row was returned"
	ErrShouldBindUri    = "uri could not bind with the struct"
	ErrPasswordMistMach = "password miss match"
)
